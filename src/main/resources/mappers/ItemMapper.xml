<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.unisabana.dyas.sampleprj.dao.mybatis.mappers.ItemMapper">

    <!-- Mapeo de la entidad Item -->
    <resultMap id="ItemResult" type="edu.unisabana.dyas.samples.entities.Item">
        <id property="id" column="ID_Item"/>
        <result property="nombre" column="Nombre_Item"/>
        <result property="descripcion" column="Descripcion_Item"/>
        <result property="fechaLanzamiento" column="fechalanzamiento"/>
        <result property="tarifaxDia" column="tarifaxdia"/>
        <result property="formatoRenta" column="formatorenta"/>
        <result property="genero" column="genero"/>
        
        <!-- Relación con TipoItem -->
        <association property="tipoItem" javaType="edu.unisabana.dyas.samples.entities.TipoItem" resultMap="edu.unisabana.dyas.sampleprj.dao.mybatis.mappers.TipoItemMapper.TipoItemResult"/>
    </resultMap>

    <!-- Consultar un Item por ID -->
    <select id="consultarItem" parameterType="int" resultMap="ItemResult">
        SELECT 
            i.id AS 'ID_Item',
            i.nombre AS 'Nombre_Item',
            i.descripcion AS 'Descripcion_Item',
            i.fechalanzamiento,
            i.tarifaxdia,
            i.formatorenta,
            i.genero,
            ti.id AS 'ID_Tipo_Item',
            ti.descripcion AS 'Descripcion_Tipo_Item'
        FROM VI_ITEMS i
        LEFT JOIN VI_TIPOITEM ti ON i.TIPOITEM_id = ti.id
        WHERE i.id = #{id};
    </select>

    <!-- Consultar todos los Items -->
    <select id="consultarItems" resultMap="ItemResult">
        SELECT 
            i.id AS 'ID_Item',
            i.nombre AS 'Nombre_Item',
            i.descripcion AS 'Descripcion_Item',
            i.fechalanzamiento,
            i.tarifaxdia,
            i.formatorenta,
            i.genero,
            ti.id AS 'ID_Tipo_Item',
            ti.descripcion AS 'Descripcion_Tipo_Item'
        FROM VI_ITEMS i
        LEFT JOIN VI_TIPOITEM ti ON i.TIPOITEM_id = ti.id;
    </select>

    <!-- Insertar un Item -->
    <insert id="insertarItem">
        INSERT INTO VI_ITEMS (id, nombre, descripcion, fechalanzamiento, tarifaxdia, formatorenta, genero, TIPOITEM_id)
        VALUES (#{id}, #{nombre}, #{descripcion}, #{fechaLanzamiento}, #{tarifaxDia}, #{formatoRenta}, #{genero}, #{tipoItem.id});
    </insert>

</mapper>
